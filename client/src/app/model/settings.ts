export class Settings {
    tickRates: number;
    hostName: string;
    numRamPackets: number;
    numInitPackets: number;
    startPort: number;
    overweight: boolean[];
    numPorts: number;
    isInitiator: boolean;
    numThreads: number;
}